CREATE DATABASE IF NOT EXISTS covid_database; -- Create a Database if does not exist
USE covid_database;    -- use the newly created Database
show databases;   -- show all the Databases 
select database();   -- Show the Current Database Used